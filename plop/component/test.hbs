import React from 'react';
import { render, screen } from '@testing-library/react';

import {{properCase name}} from '.';

describe('{{properCase name}} component renders correctly', () => {
  it('renders {{properCase name}}', () => {
    render(<{{properCase name}} />);

    const {{camelCase name}}Node = screen.getByTestId('{{dashCase name}}-item');

    expect({{camelCase name}}Node).toBeInTheDocument();

    expect({{camelCase name}}Node).toMatchSnapshot();

    {{#if mock}}
    expect({{camelCase name}}Node).toHaveClass('{{camelCase name}}');
    {{/if}}
  })
})